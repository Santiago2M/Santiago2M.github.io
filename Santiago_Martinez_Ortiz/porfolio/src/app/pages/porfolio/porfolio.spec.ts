import { PorfolioComponent } from './porfolio';
describe('PorfolioComponent', () => {
  it('should create', () => {
    const component = new PorfolioComponent();
    expect(component).toBeTruthy();
  });
});
